# Curator

## Local Development Quickstart

### Setup
```bash
gh repo clone ericcecchi/curator-backend
cd curator-backend
cp .env.example .env
docker-compose pull
```

### Run
```bash
docker-compose up -d
```

Visit http://localhost:8055

